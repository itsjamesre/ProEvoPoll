        <div class="results">
            <h2>{{ title }}</h2>
            <fieldset class="ratings">
                <div id="plotRatings-{{ id }}" style="height:200px"></div>
            </fieldset>

            <fieldset class="multiple">
                <div id="plotMultiple-{{ id }}" style="height:200px;">{{ id }}</div>
            </fieldset>
        </div>